import java.util.HashMap;

public class FileHelper {
    public static HashMap<String, String> readSlangWord() {
        HashMap<String, String> slangs = new HashMap<>();
        slangs.put("key", "value");
        slangs.put("key1", "key value 1");
        slangs.put("key2", "val be value2");
        return slangs;
    }
}
